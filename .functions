function proj() {
    if [ -z "$1" ]; then
        echo "Provide a project name";
        return;
    fi

    if [ "$1" == "test" ] || [ "$1" == "test" ]; then
        cd ~/Sites/test || return;
    fi
}
